/*
    CONSTRUCTOR 
    ==============
    if we dont give any value to object(for setter) then it will take junk value so avoid that we use constructor
    use like no-args constructor
    special function which  
    syntax:
        Class_name(){                 

        }
    
    *used to give a default vaLUE
    *automatic call the constroctor when object created
    *doesnot contain return type
    *contains same name as class name
    *we use public specifier for constructor

    TYPES:
    =======
    1) default constructor 
        ==> no-args constructor
    2)parameter constructor
        ==> constructor contains some parameter
    3)copy constructor
        ==> u can give/cpy the value to object by refering the antother object
*/

/*
FUNCTIONS:
===========
    function overloading  ==> same function name with different parameter ex: calcu the area of triangle ,square,rectangle
    constructor overloading == >same constructor name and different parmeters

DESTRCUTOR
===========
    ==> AFTER FUNCTION /program/class completeed its task done it wiil call automically
    ==> used to clear memory which is occupyied by constructor 
    ==> same as class name  with ~ ==> ~class_name();
           ~Add();
*/