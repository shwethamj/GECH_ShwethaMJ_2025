create respiratory public
open a desktop and on path type cmd

git clone " git path" 
it create a new folder shwetha_gech_2025

next cut your html and css folder and paste in new created folder shwetha_GECH_2025

git status

git add . (file add and trackind)

git commit -m "initial comit"
	#if commit not work then use below #
	global user.email "shwethamj.parvam@gmail.com"
	global user.name"shwetha mj"

git status
it show tree clean

git push origin main
then u can see your files in github

