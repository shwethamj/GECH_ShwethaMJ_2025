
user table and address table in ( project dbrelation):
=====================================================
	
	*casacde = casacde .all() -- used to if one table is saved and another which conncetd is also save and delete also work same 
	*onetoone attribute used to connect tables
	*mapping = "user" (user is name given for column name in address table and owner tabel)
		--by mapping we dont wnat to create extra column for a address-id in user table ,the user_is column created in address table is only show in table
		--